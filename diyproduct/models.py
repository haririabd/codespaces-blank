from django.db import models

# Create your models here.

#class UploadedHandheld(models.Model):
#    file = models.FileField(upload_to='uploads/')  # Adjust 'uploads/' as needed
#    uploaded_at = models.DateTimeField(auto_now_add=True)
    # Add other fields as required (e.g., user, description)